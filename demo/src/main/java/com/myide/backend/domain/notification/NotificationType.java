package com.myide.backend.domain.notification;

public enum NotificationType {
    SCHEDULE,
    CHAT,
    GIT_COMMIT,
    GIT_PUSH,
    BOARD_POST,
    BOARD_COMMENT,
    DEVLOG,
    INVITE,
    SYSTEM,
    ERROR
}